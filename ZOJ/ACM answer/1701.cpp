#include<stdio.h>

struct STORE
{
	int cl,rt;
};

int main()
{
	int widht;
	STORE	inp[1001];
	int cnt;

	while(scanf("%d",&widht)!=EOF)
	{
		if(width==0)	break;
		cnt=0;
		while(scanf("%d %d",&inp[cnt].cl,&inp[cnt].rt))
		{
			cnt++;
			if(inp[cnt].cl==0&&inp[cnt].rt==0)	break;
		}
		int i=0;

		

		