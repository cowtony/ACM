# Assignment:

```C++
class Vehicle {
    // Add brand, model, year
    // Create constructor, and necessary getter / setter
};

class Car : public Vehicle {
    // Add fuelType, mileage
    // Create constructor, and necessary getter / setter
};

class Motorcycle : public Car {
    // Add engineType, topSpeed
    // Create constructor, and necessary getter / setter
};
```